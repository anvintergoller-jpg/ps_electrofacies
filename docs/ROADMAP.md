# План работ

Проект: типизация пластов по кривой ПС.
Обновляется по мере продвижения.

## Сделано

- ✅ **Итерация 0.0**: чтение LAS, .dev, WellTops (Excel)
- ✅ **Итерация 0.1**: нормализация кривых, единая таблица,
  визуализация с осями в TVDSS
- ✅ **Шаг A**: формализация признаков (`docs/FEATURES.md`)
- ✅ **Шаг B**: признаки пласта-контейнера (уровень A) —
  `src/domain/features.py`
- ✅ **Шаг C**: сегментация пласта на коллектор / неколлектор —
  `src/domain/segmentation.py`
- ✅ **Шаг D.1**: единый порог коллектора SP_norm < 0.6
- ✅ **Шаг D.2**: литологический профиль (5 долей αПС)
  в `features.py`
- ✅ **Шаг D.3**: классификатор формы аномалии ПС —
  `src/domain/classify_form.py`, версия правил 2.0, 6 типов
  (`bell`, `funnel`, `cylinder`, `trapezoid-middle`, `symmetric`,
  `uncertain`). См. ADR-001 в `docs/DECISIONS_LOG.md`.
- ✅ **Шаг D.4**: визуализация формы — раскраска интервалов по
  типу, эталонные кривые на треке SP, справочная легенда форм
  (`form_legend.py`, `reference_curve.py`).
- ✅ **Шаг E**: категории размера (small / medium / large по доле
  в контейнере) и положение в контейнере (top / middle / bottom) —
  `src/domain/size_class.py`.

## Текущий шаг

**Шаг F. Сводная типизация пласта.**

Собрать всё, что уже посчитано, в одну характеристику пласта:

- количество коллекторных интервалов,
- доминирующая форма,
- доминирующий размер,
- доминирующее положение,
- NTG пласта (доля коллектора в мощности пласта, в АО),
- тип пласта в виде короткой метки:
  - `single-bell-medium-top` — один колокол в кровле,
  - `multi-cylinder-middle` — несколько цилиндров в середине,
  - `no-sand` — глинистый пласт без коллекторов.

См. `docs/FEATURES.md`, §6.

## Дальше по плану

- **Шаг F**: сводная типизация пласта.
- **Шаг G**: типизация разреза по скважине — последовательность
  типов пластов, характеристика разреза целиком.
- **Шаг H**: сопоставление скважин, кластеризация разрезов
  (типизация выборки). На этом шаге — возврат к доработке
  критериев классификации формы (см. ADR-002).

## Отложено

**Доработка критериев классификации формы** (ADR-002).

Система типов зафиксирована в 6 категорий (ADR-001).
Возврат к теме — после шага G, при накоплении ≥ 5 скважин
с ручной разметкой. См. `docs/DECISIONS_LOG.md`, ADR-002.

## Долгосрочные идеи

- Интеграция ГК в правила классификации (мультиметодность).
- Учёт фациальных моделей (Ежова, Муромцев) — расширение
  базовых типов формы.
- Машинное обучение — после накопления 20+ скважин с ручной
  разметкой.
- Карты электрофаций по пластам.
- Веб-интерфейс (Streamlit) для коллег-геологов.
- Пакетная обработка сотен скважин.

## Принципы работы

- Сначала методика (документ), потом код.
- Все параметры — в `config/config.yaml`, не в коде.
- Максимум комментариев в коде (проект учится с нуля).
- Не менять старое без причины.
- Каждая значимая итерация — commit + push в GitHub.
- Данные — только локально, в GitHub не попадают.