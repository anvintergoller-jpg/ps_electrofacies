# Зависимости проекта ps_electrofacies
# Установить всё:  pip install -r requirements.txt

# --- Основные библиотеки для работы с данными ---
numpy>=1.26
pandas>=2.2

# --- Чтение каротажа и Excel ---
lasio>=0.31
openpyxl>=3.1        # чтение/запись .xlsx

# --- Графики ---
matplotlib>=3.8

# --- Работа с YAML-конфигами ---
pyyaml>=6.0

# --- Позже понадобится (оставьте на будущее, пока не ставим) ---
# scipy>=1.13         # научные расчёты, асимметрия, эксцесс
# pyarrow>=16.0       # parquet для промежуточных данных